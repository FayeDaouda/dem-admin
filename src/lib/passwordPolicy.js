// Politique mot de passe (identique au backend)
export const PASSWORD_PATTERN = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$/
export const PASSWORD_HINT = '8 caracteres minimum, avec une majuscule, une minuscule et un chiffre.'
